#include <stdio.h>
#include <stdlib.h>

typedef struct bit_array {
    char* bits;
    size_t size;
} bit_array;

void set_bit(bit_array* array, size_t index) {
    
}

void unset_bit(bit_array* array, size_t index) {

}

size_t get_bit(bit_array* array, size_t index) {
    return 0;
}

int main (int argc, char* argv) {



    return EXIT_SUCCESS;
}